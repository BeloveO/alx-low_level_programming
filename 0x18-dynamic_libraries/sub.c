#include <stdio.h>
#include <stdlib.h>
/**
 * op_sub - Subtraction of two numbers
 * @a: Number
 * @b: Second Number
 * Return: Subtraction of b from a
 */
int op_sub(int a, int b)
{
	return (a - b);
}
