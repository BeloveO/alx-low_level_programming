#include <stdio.h>
/**
 * main - Prints all single digits in base 10
 *
 * Result: always 0 (success)
 */
int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
		printf("%d", x);
	printf("\n");
	return (0);
}
