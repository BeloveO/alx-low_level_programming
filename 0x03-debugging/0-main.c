#include "main.h"
/**
 * main - tests function to see if integer is positive or negative
 *
 * Return: always 0 (success)
 */
int main(void)
{
	int c;
	
	c = 0;
	positive_or_negative(c);

	return (0);
}
